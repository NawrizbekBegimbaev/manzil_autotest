================================================================================
Manzil — Bug Report (open)
================================================================================

================================================================================
CLOSED (verified 2026-05-12 by full pytest run)
================================================================================
- Legacy BUG-003: cross-flow email Supplier↔TK — теперь блокируется (закрыто).
- Legacy BUG-005: duplicate TIN в /suppliers и cross-flow Supplier↔TK —
  теперь возвращает 409 (закрыто).
- Legacy BUG-007 / current BUG-009 supplier duplicate phone check — теперь
  блокируется для supplier registration (закрыто для покрытого тестом flow).

Тесты, которые это подтверждают, теперь без xfail; следующий прогон должен
показать их в passed без xpassed.

================================================================================
CLOSED (verified 2026-05-13 by BRD §4 E2E run × 2 retries, 27s each)
================================================================================
- BUG-004: SUPPLIER_ADMIN получает 403 на POST /orders/{id}/offers/{id}/select.
  Закрыто — E2E шаг 12 (Supplier выбирает победителя) проходит. Backend починил
  RBAC, ADMIN теперь имеет право выбирать победителя как по матрице BRD §3.
- BUG-008 (P0): TK_ADMIN JWT отвергался с 401 на /me, /my-offers, /feed.
  Закрыто — E2E шаг 8 (TK видит заявку в /feed) проходит. JWT принимается,
  TK получает полный список feed-заявок.
- BUG-010: TK web-registration OTP email не приходил в Gmail IMAP за 180s.
  Закрыто как non-reproducible — single flake одного прогона. На двух
  последующих retry письмо приходит за <30s каждый раз, весь E2E
  отрабатывает за 27 секунд.

BUG-007 (TK /offers крашится 404 на оффер удалённой заявки) разблокирован
после закрытия BUG-008 — нужна отдельная верификация UI-сценария.

================================================================================
CLOSED (verified 2026-05-13 by /tmp/probe_all_bugs.py)
================================================================================
- BUG-009: cross-flow phone Supplier→TK теперь возвращает 409 «Пользователь
  с таким номером телефона уже существует». Закрыто.

Также пере-верифицировано (по-прежнему открыто): BUG-001, BUG-002.
Существенно изменилось: BUG-003 (см. ниже — направление дрифта поменялось).

================================================================================
OBSERVATIONS (mobile W4 2026-05-14 — to investigate, not yet confirmed bugs)
================================================================================
- Mobile submit offer with price=0 → backend accepts (mutation test
  submit_zero_price passed without form re-open or error). Product question:
  should a zero-price offer be valid?
- Mobile submit offer with price=999999999 → backend accepts (mutation test
  submit_large_price passed). Product question: should there be an upper
  bound?

Both observations need product/backend confirmation before deciding if
they are bugs. Tests pass either way (form closes on submit).

Дата обнаружения:   2026-04-30 (initial)
Re-verified:        2026-05-13 (probe + E2E run)
Окружение:          dev (architecture: same-origin)
                    API + UI:   https://dev-manzil.greatmall.uz
                    Swagger UI: https://dev-manzil.greatmall.uz/swagger-ui
                    Auth:       https://dev-auth.greatmall.uz (Keycloak,
                                realm `manzil-dev`)
HTTP-клиент:        httpx 0.27 (Python 3.13), JSON over TLS 1.3
Локаль:             ru-RU
Аккаунты:           Anonymous + supplier admin/manager/dispatcher (TeamQa) +
                    TK admin
Обнаружено:         Автоматизированный API/UI test suite
                    (pytest + httpx + pydantic v2 + Playwright).

Этот файл содержит ТОЛЬКО открытые баги. Закрытые удалены из тела документа
(их короткая история сохранена в верхних CLOSED-секциях). Номера НЕ
переиспользуются — gaps в нумерации (BUG-004, BUG-008, BUG-010 отсутствуют)
это норма, как в Jira/Linear; legacy-ссылки в xfail-сообщениях кода остаются
валидными.

Текущие открытые: BUG-001, -002, -003, -005, -006, -007 (6 шт).
Re-verified open 2026-05-13: BUG-001, BUG-002, BUG-003, BUG-005, BUG-007.
Не верифицировано: BUG-006 (требует Playwright).


================================================================================
BUG-001: Регистрация с дублирующимся email возвращает 204 вместо 409
================================================================================

Severity:           Major
Priority:           P1
Компонент:          Backend (auth-module/suppliers — отсутствует уникальность
                    email в рамках одного flow)

Описание:
    Повторная регистрация Supplier Admin с тем же email (но другим phone)
    возвращает 204 No Content вместо ожидаемого 409 Conflict.

    Странность: в текущем dev backend закрыты соседние uniqueness-проверки —
    дубль email cross-flow (supplier→TK) даёт 409, дубль phone в /suppliers
    даёт 409, дубль TIN cross-flow даёт 409. А вот этот один сценарий —
    дубль email в одном /suppliers с другим phone — всё ещё 204.

    По swagger-контракту 409 описан явно:
    "Email already registered or TIN already in use"

Шаги воспроизведения:
    1. POST /api/v1/auth/web/registrations/suppliers
       {"email":"dup@manziltest.uz", "tin":"200111222333",
        "phone":"+998900000010", ...} → 204
    2. POST /api/v1/auth/web/registrations/suppliers
       {"email":"dup@manziltest.uz", "tin":"200111222444",
        "phone":"+998900000011", ...}

Ожидаемый: Шаг 2 → 409 (Email уже зарегистрирован)
Фактический: Шаг 2 → 204 No Content (re-verified 2026-05-05)

Доказательство:
    - tests/auth/test_web_register_supplier.py::
        test_register_supplier_with_duplicate_email_returns_409
    - /tmp/bug_probe2.py (BUG-004 v2)

Возможные интерпретации:
    (а) БАГ: backend забыл проверить уникальность только для этой комбинации.
    (б) ФИЧА: повторный POST триггерит resend OTP (идемпотентность).
        Тогда swagger описывает несуществующий 409 — нужно обновить.

Рекомендация:
    Backend lead должен решить:
        1. Если "идемпотентный resend" — задокументировать в swagger,
           убрать 409 из responses.
        2. Если уникальность нужна — добавить SELECT по email перед INSERT,
           вернуть 409 на дубль (как уже сделано для phone и cross-flow email).


================================================================================
BUG-002: Swagger UI и /v3/api-docs скрыты за oauth2-proxy (realm `grafana`)
================================================================================

Severity:           Minor (информация / неудобство для CI)
Priority:           P2
Компонент:          Infrastructure (oauth2-proxy конфиг)

Описание:
    Прямой запрос к https://dev-manzil.greatmall.uz/v3/api-docs возвращает
    302 → https://auth.greatmall.uz/realms/grafana/protocol/openid-connect/auth.

    Два момента:
    1. Realm в редиректе называется `grafana`, хотя swagger описывает
       JWT-токены приложения как issued от realm `manzil-dev`. Кажется, dev
       переиспользует существующий realm для SSO-фронта.
    2. CI / постман / contract-validator не могут получить openapi.json
       без manual SSO-логина в браузере.

Доказательство:
    - tests/smoke/test_health.py (skip с причиной "gated by oauth2-proxy")
    - curl -sSI https://dev-manzil.greatmall.uz/v3/api-docs → 302 на
      auth.greatmall.uz/realms/grafana/...
    - re-verified 2026-05-05: всё ещё 302.

Рекомендация:
    1. Подтвердить, какой realm должен быть для приложения — `manzil-dev`
       или `grafana`. Если `manzil-dev` — переконфигурировать oauth2-proxy.
    2. /v3/api-docs — read-only OpenAPI спека, безопасно отдавать
       публично. Открыть его (и /swagger-ui/) для не-залогиненных юзеров
       либо выдать service-account для CI.


================================================================================
BUG-003: Контракт-дрифт ИНН UI ↔ API (направление поменялось 2026-05-13)
================================================================================

Severity:           Major (UI может пропустить ИНН, который backend отвергнет)
Priority:           P1
Компонент:          Frontend + Swagger (контракт-документация)
Re-verified:        2026-05-13 (/tmp/probe_all_bugs.py)

Текущее состояние (verified 2026-05-13):
    API принимает **только TIN длиной 12 символов**. Любое другое
    значение (1, 5, 7, 8, 18, 19 — пробовали) → 400 Bad Request.
    UI всё ещё валидирует диапазон 8–18.

    То есть направление дрифта **поменялось**: раньше UI был уже API
    (UI 8-18 vs API 1-18), теперь UI шире API (UI 8-18 vs API ровно 12).

Новое следствие:
    Пользователь с TIN 8-11 или 13-18 цифр пройдёт UI-валидацию, отправит
    форму, получит 400 «Неверный запрос» от бекенда. Сообщение об ошибке
    generic, не указывает на поле tin.

Swagger требуется обновить:
    Старый паттерн `^[0-9]{1,18}$` больше не отражает реальность.
    Должно быть `^[0-9]{12}$`.

Рекомендация:
    1. Backend: оставить как есть (12 — стандарт UZ ИНН).
    2. UI: сузить валидацию формы до ровно 12 цифр, добавить
       contextual error «ИНН должен содержать ровно 12 цифр».
    3. Swagger: обновить `tin` pattern на `^[0-9]{12}$`.

Доказательство:
    - /tmp/probe_all_bugs.py BUG-003 (probe 2026-05-13):
      Accepted lengths: [12]
      Rejected lengths: [1, 5, 7, 8, 18, 19] (все 400)
    - web_ui/supplier/auth/test_pre_otp_register.py — UI блокирует submit < 8

Old description (kept for traceability):
    На /auth/register/supplier UI блокировал submit при длине ИНН < 8.
    Backend по swagger принимал `^[0-9]{1,18}$`. Re-recon 2026-05-05: drift
    сохранялся (UI 8-18 vs API 1-18).
    Сейчас два слоя противоречат друг другу.


================================================================================
BUG-005: MZL-номера заявок не уникальны глобально между компаниями
================================================================================

Severity:           Minor (UX неоднозначность)
Priority:           P3
Компонент:          Backend (нумерация заявок)
Re-verified:        2026-05-13 (/tmp/probe_remaining_bugs.py BUG-005)
                    Probe создал 2 ордера в 2 разных компаниях, оба получили
                    'MZL-0001'. Подтверждено что бекенд использует per-company
                    sequence, не глобальный.

Описание:
    MZL-номер (MZL-NNNN) генерируется per-company (счётчик у каждого
    поставщика начинается с 0001). В TK-ленте /feed одновременно
    видны заявки от разных поставщиков, что может приводить к
    нескольким одновременно видимым «MZL-0012» от разных компаний.

    Не критично (заявки в БД идентифицируются UUID), но в UI
    создаёт неоднозначность — пользователь не может сослаться на
    «MZL-0012» без указания компании.

Доказательство:
    - web_ui/e2e/test_tender_flow_cross_role.py — пришлось искать
      созданную заявку по уникальному cargo_type, а не MZL-номеру.
    - В live-данных на dev уже видны >1 строки «MZL-0001»,
      «MZL-0002», «MZL-0003» от разных suppliers.

Рекомендация:
    Один из вариантов:
    1. Глобальный счётчик MZL-номеров (просто, но «теряет красоту» seq).
    2. Префикс компании в номере: «TQ-MZL-0012» / «AC-MZL-0012» где
       TQ/AC — короткий код компании.
    3. Скрыть MZL в TK-ленте, использовать только UUID-короткий.


================================================================================
BUG-006: На «В работе» заявке UI не показывает action-кнопки матрицы
================================================================================

Severity:           Major (заблокированный workflow)
Priority:           P1
Компонент:          Frontend (страница /orders/{id})

Описание:
    Страница заявки в статусе «В работе» по матрице должна показывать
    у ADMIN и MANAGER:
    - кнопку «Завершить» (перевод в «Завершена»)
    - кнопку «Отменить»
    - на каждом оффере: кнопку «Заметка»
    - кнопку «Выбрать победителя» (если ещё не выбран)

    Реальность (наблюдалось в MCP-сессии 2026-05-03):
    - У ADMIN на «В работе»: только «Назад». Никаких других кнопок.
    - У MANAGER на «В работе»: «Назад» + «Заметка» (на оффере).
      Никаких «Завершить»/«Отменить»/«Выбрать победителя».

    То есть весь activeFlow тендера (после выбора победителя)
    выполним только через прямые curl-вызовы API. Это критический
    UX-блокер: ни один пользователь не сможет завершить заявку
    через UI.

    Re-recon 2026-05-05: probe не получилось (нет «В работе» заявки
    в текущих данных), но статус не помечен как закрытый — нужна
    повторная UI-рекон при появлении такой заявки.

Доказательство:
    - web_ui/supplier/orders/test_order_detail.py — тесты на статус-
      badge и presence/absence кнопок видят только «Назад».
    - API endpoints (`/orders/{id}/complete`, `/cancel`, `/select`,
      `/offers/{id}/note`) при этом работают и покрыты API-тестами.

Рекомендация:
    Добавить в /orders/{id} рендер action-buttons по статусу + role:
    - «Активна» + ADMIN/MANAGER → «Отменить»
    - «В работе» + ADMIN/MANAGER → «Завершить» + «Отменить»
    - «Активна» с офферами + ADMIN/MANAGER → «Выбрать победителя»
    - DRAFT + own DISPATCHER → «Опубликовать», «Редактировать»
    - На каждом оффере, ADMIN+MANAGER → «Заметка»

    После починки добавить ~10 UI-тестов на эти flow.


================================================================================
BUG-007: TK /offers ломается на «404 Заявка не найдена», если есть оффер на
         удалённую/отменённую заявку
================================================================================

Severity:           Major (страница недоступна для пользователя)
Priority:           P1
Компонент:          **Frontend only** (verified 2026-05-13)
Re-verified:        2026-05-13 (/tmp/probe_remaining_bugs.py BUG-007)

Состояние backend (verified 2026-05-13):
    GET /api/v1/my-offers возвращает **200** с paginated content. В items
    есть нужные поля для рендеринга, включая `orderStatus` (CANCELLED для
    отменённых заявок) — фронт может узнать состояние не делая отдельный
    запрос за заявкой.

    Probe-результат:
        HTTP 200
        items[0] = {
            "id": "<offer-uuid>",
            "status": "NOT_SELECTED",     # backend автоматически сменил
            "orderStatus": "CANCELLED",   # денормализован в response
            ...
        }

    То есть backend полностью даёт фронту нужное чтобы НЕ падать. Это
    100% frontend bug.

Шаги воспроизведения (UI):
    1. TK подаёт оффер на заявку поставщика
    2. Поставщик-admin отменяет заявку (status=CANCELLED)
    3. TK открывает /offers
    4. Страница показывает «404. Заявка не найдена» вместо таблицы

Доказательство:
    - /tmp/probe_remaining_bugs.py BUG-007: API даёт 200 + полные данные
    - UI на /offers рендерит 404 (frontend fetch'ит каждую заявку отдельно
      и не обрабатывает 404)

Рекомендация:
    Frontend: не делать отдельный fetch заявки в /offers рендере. Использовать
    `orderStatus` поле из my-offers response. Если оно `CANCELLED` —
    показывать строку с серым плейсхолдером «Заявка отменена», offer
    данные всё ещё валидны.

    Backend часть работы — уже сделана (`orderStatus` денормализован).


[Полное описание BUG-009 удалено 2026-05-13 — баг закрыт. Короткая запись в
верхней CLOSED-секции; см. probe в /tmp/probe_all_bugs.py.]


================================================================================
Сводка
================================================================================

BUG-001  Major      P1   Дубль email в /suppliers возвращает 204 вместо 409 — re-verified open 2026-05-13
BUG-002  Minor      P2   Swagger UI за oauth2-proxy (теперь /oauth2/start) — re-verified open 2026-05-13
BUG-003  Major      P1   ИНН: API теперь требует ровно 12, UI всё ещё 8-18 — direction changed (re-verified 2026-05-13)
BUG-005  Minor      P3   MZL-номера не уникальны cross-company (per-company seq) — re-verified open 2026-05-13
BUG-006  Major      P1   UI не показывает action-кнопки на «В работе» — не верифицирован (требует Playwright)
BUG-007  Major      P1   TK /offers 404 — Frontend-only (API дал backend часть, orderStatus в response) — re-verified 2026-05-13


================================================================================
Открытые вопросы к команде backend
================================================================================

Q1.  Точные правила TIN: только 12 цифр? Есть ли checksum по алгоритму ГНК?
     (uniqueness уже enforce'ится — см. ранее закрытые баги)

Q2.  Какой realm правильный для приложения — `manzil-dev` (issuer JWT) или
     `grafana` (oauth2-proxy)?

Q3.  Можно ли получить заголовок типа X-Test-Clock для time-based тестов
     (24h invitation TTL, 5 min mobile-verify window)?

Q4.  BUG-008: была ли TK_ADMIN роль удалена из allow-list какого-то
     SecurityFilter недавно? Какой commit / change request это сделал?
     [Возможно резолвед — BUG-008 закрыт 2026-05-13. Если backend знает что
      именно правил — задокументировать для постмортема.]

Q5.  ~~BUG-009 phone uniqueness~~ — закрыто 2026-05-13 (cross-flow Supplier→TK
     возвращает 409 "уже существует").

Q7.  POST /api/v1/orders/{id}/cancel: SUPPLIER_DISPATCHER получает 409
     "error.order.not-cancellable" если у order'а есть active offer, но
     SUPPLIER_ADMIN отменяет успешно. По BRD §3 — это feature (admin override)
     или непрописанный RBAC bug? Найдено в /tmp/probe_remaining_bugs.py
     2026-05-13.

Q6.  BUG-010 закрыт как non-repro flake. Но **Telegram OTP** для mobile-driver
     registration всё ещё блокирует ~3 теста. Просьба: dev-only endpoint
     `GET /dev/last-otp/{phone}` (или фиксированный код для тест-номеров).
     См. `CODEX_NEXT.md` N4 / Q3 (Telegram OTP в CLAUDE.md).
